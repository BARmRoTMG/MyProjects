<Page 
    x:Class="UI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    NavigationCacheMode="Required">

    <Grid x:Name="grid">
            
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10, 0, 10, 0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="btn" Content="Customer" Width="Auto" Margin="10, 0, 0, 10" Background="White" Foreground="Black" FontSize="30" Click="btn_Click"/>
            <Button x:Name="btn2" Content="Librarian" Width="Auto" Margin="10, 0, 0, 10" Background="White" Foreground="Black" FontSize="30" Click="btn2_Click"/>
        </StackPanel>

        <Frame Background="Azure" Grid.Row="1" x:Name="frame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</Page>
